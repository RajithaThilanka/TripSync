import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DashbordScreen = () => {
  return (
    <View>
      <Text>Dashbord Page</Text>
    </View>
  );
};

export default DashbordScreen;

const styles = StyleSheet.create({});
